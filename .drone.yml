pipeline:
  build:
    image: golang
    environment:
      - GOPATH=/drone
    commands:
      - mkdir -p /drone/src/badc0de.net/pkg && ln -s /drone/src/bitbucket.org/ivucica/go-tibia /drone/src/badc0de.net/pkg/go-tibia
      - go test -i badc0de.net/pkg/go-tibia/...  # install dependencies of tests
      - go test badc0de.net/pkg/go-tibia/...  # test *everything*


